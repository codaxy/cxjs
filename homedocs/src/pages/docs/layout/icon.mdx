---
layout: ../../../layouts/DocsLayout.astro
title: Icon
---

import ImportPath from "../../../components/ImportPath.astro";
import CodeExample from "../../../components/CodeExample.astro";

import IconExample from "../../../examples/layout/IconExample.tsx";
import IconExampleCode from "../../../examples/layout/IconExample.tsx?raw";

# Icon

<ImportPath path="import { Icon } from 'cx/widgets';" />

The `Icon` component is used to render icons. CxJS includes only a few built-in icons; additional icon sets need to be registered.

<CodeExample code={IconExampleCode}>
  <IconExample client:load />
</CodeExample>

## Registering Icons

Icon sets like FontAwesome can be registered using `Icon.registerFactory`:

```tsx
Icon.registerFactory((name, props) => {
  props = { ...props };
  props.className = `fa fa-${name} ${props.className || ''}`;
  return <i {...props} />
});
```

Individual icons can be registered using `Icon.register`:

```tsx
Icon.register("custom-icon", (props) => <svg {...props}>...</svg>);
```

## Configuration

| Property | Type | Description |
| -------- | ---- | ----------- |
| `name` | `string` | Name under which the icon is registered. |
| `baseClass` | `string` | Base CSS class. Default is `icon`. |
| `className` | `string` | Additional CSS class to apply to the icon. |
| `style` | `string \| object` | Additional styles to apply to the icon. |
