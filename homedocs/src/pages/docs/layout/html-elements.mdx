---
layout: ../../../layouts/DocsLayout.astro
title: HtmlElement
---

import CodePreview from "../../../components/CodePreview.astro";
import HtmlElementsExample from "../../../examples/intro/HtmlElementsExample.tsx";
import HtmlElementsExampleCode from "../../../examples/intro/HtmlElementsExample.tsx?raw";

**Import:** `import { HtmlElement } from 'cx/widgets';`

The `HtmlElement` widget is used to render HTML elements. All lowercase elements will be converted to the `HtmlElement` instances with the corresponding `tag` property set.

## Example

<CodePreview code={HtmlElementsExampleCode} filterLines={["div", "span", "p "]}>
  <HtmlElementsExample client:load />
</CodePreview>

All HTML attributes and events will be passed to the element. Cx specific attributes, such as `visible`, `layout`, `controller`, `outerLayout` and `items` work as expected.

## Configuration

For configuration options, see the HtmlElement widget documentation.
