---
layout: ../../../layouts/DocsLayout.astro
title: Drag and Drop
---

import CodeExample from "../../../components/CodeExample.astro";
import ImportPath from "../../../components/ImportPath.astro";
import DragDropExample from "../../../examples/concepts/DragDropExample.tsx";
import DragDropExampleCode from "../../../examples/concepts/DragDropExample.tsx?raw";

# Drag and Drop

<ImportPath path="import { DragSource, DropZone, DragHandle } from 'cx/widgets';" />

CxJS provides components for implementing drag and drop functionality:

- [DragSource](/docs/concepts/drag-source) - Wraps elements that can be dragged
- [DropZone](/docs/concepts/drop-zone) - Defines areas where items can be dropped
- [DragHandle](/docs/concepts/drag-handle) - Optional handle to initiate dragging

## Example

This example demonstrates list reordering using drag and drop:

<CodeExample code={DragDropExampleCode}>
  <DragDropExample client:load />
</CodeExample>

## How It Works

1. Each item is wrapped in a `DragSource` that passes the record data when dragged
2. A `DropZone` around each item receives the dropped data
3. The `onDrop` handler reorders the array by removing the item from its old position and inserting it at the new position
