import { View } from '../../data/View';
import { Instance } from '../Instance';

export function startAppLoop(parentDOMElement: Element, storeOrInstance: View | Instance, widget?: any, options?: any);